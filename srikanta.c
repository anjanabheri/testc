
#include<stdio.h>
main()
{
    char ip[]="192.168.9.2";
    a.b.5
        char *p = ip ;
    int num = 0;
    if(strlen(ip) > 15)
        printf("invalid\n");
    while(p)
    {
        while(*p != '.' )
        {
            if(!(*p >= 'a' || *p <= 'z'))
                num = (num * 10) + atoi(*p);
            p++;
        }
        if(num > 255)
            printf("invalid");
        num = 0;
    }
    if(sscanf("%d"))}******************************

#include<stdio.h>
    typedef struct node
{
    struct node *next;
    struct node *prev;
    int num;
}NODE;

getNode()
{
    NODE *tmp = (NODE*)malloc(sizeof(NODE)); node->next = NULL;
    node->prev = NULL;
}
    2->3->
void insert(NODE **head,int num,int pos)
{
    NODE *tmp = *head;
    NODE *node = getNode(num);
    NODE *prev = *head;
    if (pos == 0)
    {
        node->next = *head;
        *head = node;
    }
    else
    {
        while(tmp)
        {
            if(count != pos)
            {
                count++;
                prev = tmp;
                tmp=tmp->next;
            }
            else
            {
                //
                node->next = tmp->next;
                node->prev = tmp;
                tmp->next = node;
                //
                break;
            }
        }
    }while()
    {
    }
}
main()
{
    NODE *root = NULL;
}
***********************************

int arr[]={2,3,10,1,5,4}; 1,2,3,4,5,10;
int tar_sum = 7;
STATUS find_sum(arr,tar_sum,&index1,&index2)

    *****************************************
    int num0010
    0100
    10000000 0110
    1111 1111
while(num)
{
    num = num & (num-1);
    count ++;
}
if(count == 1)
{
    printf("")
}
1010
10011000
01110000
0100 0000
00
6110
101100
011000111
110110
101100
0110


